import Layout from '@/components/features/Layout';
import OrdersHistoryPage from '@/components/pages/OrdersHistory';

const OrdersHistory = () => {
  return (
    <Layout>
      <OrdersHistoryPage />
    </Layout>
  );
};

export default OrdersHistory;
