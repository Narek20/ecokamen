import Layout from '@/components/features/Layout';
import HomeComponent from '@/components/pages/Home';

const Home = () => {
  return (
    <Layout>
      <HomeComponent />
    </Layout>
  );
};

export default Home;
